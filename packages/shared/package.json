{
  "name": "@regle/shared",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@regle/core": "workspace:*",
    "tsdown": "catalog:",
    "type-fest": "catalog:",
    "typescript": "catalog:",
    "vue": "catalog:"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "tsdown --config=tsdown.dev.ts --watch",
    "test": "echo 'no tests'",
    "lint": "eslint utils/**/*.ts",
    "lint:fix": "eslint utils/**/*.ts --fix"
  }
}
